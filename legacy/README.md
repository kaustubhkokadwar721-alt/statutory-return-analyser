# Legacy Material

The supported product is the browser application in `../gstr_web/`. Its parser source
in `../gstr_web/engine/` is the only maintained engine.

This folder is retained for reference only:

```
desktop-analyser/          former desktop Python application
powerquery/                former Power Query scripts
Compliance_Extractor.xlsx  former workbook/template
```

Do not synchronise these copies with the browser engine. Changes belong in the web
engine and its tests unless a separate desktop product is intentionally revived.
